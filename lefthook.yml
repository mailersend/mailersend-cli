pre-commit:
  commands:
    fmt:
      glob: "*.rs"
      run: cargo fmt
      stage_fixed: true
    lint:
      glob: "*.rs"
      run: cargo clippy --all-targets -- -D warnings

remotes:
  - git_url: git@github.com:mailerlite/git-hooks.git
    ref: main
    configs:
      - gitleaks.yml
